# Requirements for Render.com deployment
# Optimized for free tier (no heavy ML frameworks)

# Core
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
xgboost>=1.7.0,<2.1

# API
fastapi>=0.95.0
uvicorn>=0.22.0

# Data fetching
yfinance>=0.2.0
requests>=2.28.0
httpx>=0.23.0

# Scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Utils
python-dotenv>=1.0.0
joblib>=1.2.0

# LSTM ensemble (optional - graceful fallback to Ridge+XGB if unavailable)
tensorflow>=2.15.0
