# Requirements for Render.com deployment
# Optimized for free tier (no heavy ML frameworks)

# Core
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# API
fastapi>=0.95.0
uvicorn>=0.22.0

# Data fetching
yfinance>=0.2.0
requests>=2.28.0

# Utils
python-dotenv>=1.0.0
joblib>=1.2.0

# Note: TensorFlow is NOT required for Ridge Regression model
# This makes deployment much faster and lighter
